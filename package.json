{
  "name": "design-skills-dev",
  "private": true,
  "description": "Superpowers-compatible skills for the design-feature workflow (dev-only package.json — distribution is the git repo).",
  "type": "module",
  "scripts": {
    "build:template": "node scripts/build-template.mjs",
    "test": "node validate.mjs && node scripts/smoke-test.mjs",
    "validate": "node validate.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
